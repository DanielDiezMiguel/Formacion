version: "3.3"
services:
  back-empresa:
    build: .
    ports:
      - 8090:8090
    networks:
      - mynetwork

  mongo:
    image: mongo
    ports:
      - 9001:27017
    networks:
      - mynetwork
    environment:
      - MONGO_INITDB_ROOT_USERNAME=daniel
      - MONGO_INITDB_ROOT_PASSWORD=Dds9cP4zlMMIEgGQ
    volumes:
      - mongo-data:/data/db
networks:
  mynetwork:
volumes:
  mongo-data:
